using System.Collections;
using System.Collections.Generic;
using UnityEngine;
 
public class Destroy_L1 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Destruction of the game object which the script is attached to
        Destroy(gameObject);
    }
 
    // Update is called once per frame
    void Update()
    {
 
    }
}